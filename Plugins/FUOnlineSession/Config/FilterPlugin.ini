[FilterPlugin]
; 【分发配置契约】BuildPlugin 默认不会把插件 Config 目录作为额外文件复制。
; 显式列出两个 FU 所有的 ini，确保 Win64 分发包启用插件后能取得可逆的 OSS 默认层和设置默认值。
; 不列出项目 DefaultEngine.ini：它始终属于项目，插件禁用后不应残留 FU 管理配置。
/Config/Engine.ini
/Config/DefaultFUOnlineSession.ini
